branch-defaults:
  master:
    environment: jenkins-docker-maven-example-default
deploy:
  artifact: Dockerrun.aws.json
global:
  application_name: jenkins-docker-maven-example
  default_platform: 64bit Amazon Linux 2016.03 v2.1.3 running Docker 1.11.1
  default_region: us-east-1
  